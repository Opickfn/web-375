-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public.audit_reports
(
    id bigserial NOT NULL,
    auditor_id bigint NOT NULL,
    pj_area_id bigint NOT NULL,
    title character varying(150) COLLATE pg_catalog."default" NOT NULL,
    findings text COLLATE pg_catalog."default" NOT NULL,
    recommendations text COLLATE pg_catalog."default",
    audit_date date NOT NULL,
    status character varying(255) COLLATE pg_catalog."default" NOT NULL DEFAULT 'open'::character varying,
    created_at timestamp(0) without time zone,
    updated_at timestamp(0) without time zone,
    pdf_path character varying(255) COLLATE pg_catalog."default",
    location_id bigint,
    CONSTRAINT audit_reports_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.cache
(
    key character varying(255) COLLATE pg_catalog."default" NOT NULL,
    value text COLLATE pg_catalog."default" NOT NULL,
    expiration bigint NOT NULL,
    CONSTRAINT cache_pkey PRIMARY KEY (key)
);

CREATE TABLE IF NOT EXISTS public.cache_locks
(
    key character varying(255) COLLATE pg_catalog."default" NOT NULL,
    owner character varying(255) COLLATE pg_catalog."default" NOT NULL,
    expiration bigint NOT NULL,
    CONSTRAINT cache_locks_pkey PRIMARY KEY (key)
);

CREATE TABLE IF NOT EXISTS public.failed_jobs
(
    id bigserial NOT NULL,
    uuid character varying(255) COLLATE pg_catalog."default" NOT NULL,
    connection text COLLATE pg_catalog."default" NOT NULL,
    queue text COLLATE pg_catalog."default" NOT NULL,
    payload text COLLATE pg_catalog."default" NOT NULL,
    exception text COLLATE pg_catalog."default" NOT NULL,
    failed_at timestamp(0) without time zone NOT NULL DEFAULT CURRENT_TIMESTAMP,
    CONSTRAINT failed_jobs_pkey PRIMARY KEY (id),
    CONSTRAINT failed_jobs_uuid_unique UNIQUE (uuid)
);

CREATE TABLE IF NOT EXISTS public.follow_ups
(
    id bigserial NOT NULL,
    report_id bigint NOT NULL,
    created_by bigint NOT NULL,
    assigned_to_name character varying(100) COLLATE pg_catalog."default" NOT NULL,
    action_plan text COLLATE pg_catalog."default" NOT NULL,
    target_date date NOT NULL,
    status character varying(255) COLLATE pg_catalog."default" NOT NULL DEFAULT 'planned'::character varying,
    completed_at timestamp(0) without time zone,
    completion_notes text COLLATE pg_catalog."default",
    created_at timestamp(0) without time zone,
    updated_at timestamp(0) without time zone,
    CONSTRAINT follow_ups_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.gedungs
(
    id bigint NOT NULL DEFAULT nextval('jurusans_id_seq'::regclass),
    kode character varying(10) COLLATE pg_catalog."default" NOT NULL,
    nama character varying(150) COLLATE pg_catalog."default" NOT NULL,
    nama_en character varying(150) COLLATE pg_catalog."default",
    is_active boolean NOT NULL DEFAULT true,
    created_at timestamp(0) without time zone,
    updated_at timestamp(0) without time zone,
    CONSTRAINT jurusans_pkey PRIMARY KEY (id),
    CONSTRAINT jurusans_kode_unique UNIQUE (kode)
);

CREATE TABLE IF NOT EXISTS public.job_batches
(
    id character varying(255) COLLATE pg_catalog."default" NOT NULL,
    name character varying(255) COLLATE pg_catalog."default" NOT NULL,
    total_jobs integer NOT NULL,
    pending_jobs integer NOT NULL,
    failed_jobs integer NOT NULL,
    failed_job_ids text COLLATE pg_catalog."default" NOT NULL,
    options text COLLATE pg_catalog."default",
    cancelled_at integer,
    created_at integer NOT NULL,
    finished_at integer,
    CONSTRAINT job_batches_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.jobs
(
    id bigserial NOT NULL,
    queue character varying(255) COLLATE pg_catalog."default" NOT NULL,
    payload text COLLATE pg_catalog."default" NOT NULL,
    attempts smallint NOT NULL,
    reserved_at integer,
    available_at integer NOT NULL,
    created_at integer NOT NULL,
    CONSTRAINT jobs_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.location_user
(
    id bigserial NOT NULL,
    user_id bigint NOT NULL,
    location_id bigint NOT NULL,
    created_at timestamp(0) without time zone,
    updated_at timestamp(0) without time zone,
    CONSTRAINT location_user_pkey PRIMARY KEY (id),
    CONSTRAINT location_user_user_id_location_id_unique UNIQUE (user_id, location_id)
);

CREATE TABLE IF NOT EXISTS public.locations
(
    id bigserial NOT NULL,
    parent_id bigint,
    code character varying(20) COLLATE pg_catalog."default",
    name character varying(150) COLLATE pg_catalog."default" NOT NULL,
    type character varying(255) COLLATE pg_catalog."default" NOT NULL,
    category character varying(50) COLLATE pg_catalog."default",
    is_active boolean NOT NULL DEFAULT true,
    created_at timestamp(0) without time zone,
    updated_at timestamp(0) without time zone,
    CONSTRAINT locations_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.migrations
(
    id serial NOT NULL,
    migration character varying(255) COLLATE pg_catalog."default" NOT NULL,
    batch integer NOT NULL,
    CONSTRAINT migrations_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.password_reset_tokens
(
    email character varying(255) COLLATE pg_catalog."default" NOT NULL,
    token character varying(255) COLLATE pg_catalog."default" NOT NULL,
    created_at timestamp(0) without time zone,
    CONSTRAINT password_reset_tokens_pkey PRIMARY KEY (email)
);

CREATE TABLE IF NOT EXISTS public.points
(
    id bigserial NOT NULL,
    user_id bigint NOT NULL,
    report_id bigint,
    amount integer NOT NULL,
    type character varying(255) COLLATE pg_catalog."default" NOT NULL,
    description character varying(255) COLLATE pg_catalog."default" NOT NULL,
    created_at timestamp(0) without time zone,
    updated_at timestamp(0) without time zone,
    CONSTRAINT points_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.reports
(
    id bigserial NOT NULL,
    reporter_id bigint,
    kategori character varying(255) COLLATE pg_catalog."default" NOT NULL,
    lokasi character varying(255) COLLATE pg_catalog."default" NOT NULL,
    deskripsi text COLLATE pg_catalog."default" NOT NULL,
    prioritas character varying(255) COLLATE pg_catalog."default" NOT NULL,
    status character varying(255) COLLATE pg_catalog."default" NOT NULL DEFAULT 'pending'::character varying,
    bukti character varying(255) COLLATE pg_catalog."default",
    reviewed_by bigint,
    reviewed_at timestamp(0) without time zone,
    review_notes text COLLATE pg_catalog."default",
    created_at timestamp(0) without time zone,
    updated_at timestamp(0) without time zone,
    gedung_id bigint,
    location_id bigint,
    solusi text COLLATE pg_catalog."default",
    CONSTRAINT reports_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.reward_periods
(
    id bigserial NOT NULL,
    name character varying(100) COLLATE pg_catalog."default" NOT NULL,
    start_date date NOT NULL,
    end_date date NOT NULL,
    status character varying(255) COLLATE pg_catalog."default" NOT NULL DEFAULT 'active'::character varying,
    created_at timestamp(0) without time zone,
    updated_at timestamp(0) without time zone,
    CONSTRAINT reward_periods_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.ruangans
(
    id bigint NOT NULL DEFAULT nextval('program_studis_id_seq'::regclass),
    gedung_id bigint NOT NULL,
    kode character varying(10) COLLATE pg_catalog."default" NOT NULL,
    nama character varying(150) COLLATE pg_catalog."default" NOT NULL,
    jenjang character varying(10) COLLATE pg_catalog."default" NOT NULL DEFAULT 'D3'::character varying,
    is_active boolean NOT NULL DEFAULT true,
    created_at timestamp(0) without time zone,
    updated_at timestamp(0) without time zone,
    CONSTRAINT program_studis_pkey PRIMARY KEY (id),
    CONSTRAINT program_studis_kode_unique UNIQUE (kode)
);

CREATE TABLE IF NOT EXISTS public.sessions
(
    id character varying(255) COLLATE pg_catalog."default" NOT NULL,
    user_id bigint,
    ip_address character varying(45) COLLATE pg_catalog."default",
    user_agent text COLLATE pg_catalog."default",
    payload text COLLATE pg_catalog."default" NOT NULL,
    last_activity integer NOT NULL,
    CONSTRAINT sessions_pkey PRIMARY KEY (id)
);

CREATE TABLE IF NOT EXISTS public.users
(
    id bigserial NOT NULL,
    full_name character varying(100) COLLATE pg_catalog."default" NOT NULL,
    email character varying(100) COLLATE pg_catalog."default" NOT NULL,
    password character varying(255) COLLATE pg_catalog."default" NOT NULL,
    role character varying(50) COLLATE pg_catalog."default" NOT NULL DEFAULT 'reporter'::character varying,
    user_type character varying(255) COLLATE pg_catalog."default" NOT NULL DEFAULT 'umum'::character varying,
    phone character varying(20) COLLATE pg_catalog."default",
    nim character varying(20) COLLATE pg_catalog."default",
    kelas character varying(20) COLLATE pg_catalog."default",
    gedung character varying(100) COLLATE pg_catalog."default",
    ruangan character varying(100) COLLATE pg_catalog."default",
    tahun_angkatan character varying(4) COLLATE pg_catalog."default",
    nomor_dosen character varying(30) COLLATE pg_catalog."default",
    jabatan character varying(100) COLLATE pg_catalog."default",
    email_verified_at timestamp(0) without time zone,
    remember_token character varying(100) COLLATE pg_catalog."default",
    created_at timestamp(0) without time zone,
    updated_at timestamp(0) without time zone,
    two_factor_secret text COLLATE pg_catalog."default",
    two_factor_recovery_codes text COLLATE pg_catalog."default",
    two_factor_confirmed_at timestamp(0) without time zone,
    gedung_id bigint,
    show_name_on_landing boolean NOT NULL DEFAULT true,
    CONSTRAINT users_pkey PRIMARY KEY (id),
    CONSTRAINT users_email_unique UNIQUE (email),
    CONSTRAINT users_nim_unique UNIQUE (nim),
    CONSTRAINT users_nomor_dosen_unique UNIQUE (nomor_dosen)
);

CREATE TABLE IF NOT EXISTS public.warnings
(
    id bigserial NOT NULL,
    created_by bigint NOT NULL,
    report_id bigint,
    title character varying(255) COLLATE pg_catalog."default" NOT NULL,
    description text COLLATE pg_catalog."default" NOT NULL,
    severity character varying(255) COLLATE pg_catalog."default" NOT NULL DEFAULT 'medium'::character varying,
    status character varying(255) COLLATE pg_catalog."default" NOT NULL DEFAULT 'active'::character varying,
    is_public boolean NOT NULL DEFAULT true,
    expires_at timestamp(0) without time zone,
    image_path character varying(255) COLLATE pg_catalog."default",
    image_source character varying(255) COLLATE pg_catalog."default" NOT NULL DEFAULT 'manual'::character varying,
    created_at timestamp(0) without time zone,
    updated_at timestamp(0) without time zone,
    is_default boolean NOT NULL DEFAULT false,
    CONSTRAINT warnings_pkey PRIMARY KEY (id)
);

ALTER TABLE IF EXISTS public.audit_reports
    ADD CONSTRAINT audit_reports_auditor_id_foreign FOREIGN KEY (auditor_id)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.audit_reports
    ADD CONSTRAINT audit_reports_location_id_foreign FOREIGN KEY (location_id)
    REFERENCES public.locations (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE SET NULL;


ALTER TABLE IF EXISTS public.audit_reports
    ADD CONSTRAINT audit_reports_pj_area_id_foreign FOREIGN KEY (pj_area_id)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.follow_ups
    ADD CONSTRAINT follow_ups_created_by_foreign FOREIGN KEY (created_by)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.follow_ups
    ADD CONSTRAINT follow_ups_report_id_foreign FOREIGN KEY (report_id)
    REFERENCES public.reports (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.location_user
    ADD CONSTRAINT location_user_location_id_foreign FOREIGN KEY (location_id)
    REFERENCES public.locations (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.location_user
    ADD CONSTRAINT location_user_user_id_foreign FOREIGN KEY (user_id)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.locations
    ADD CONSTRAINT locations_parent_id_foreign FOREIGN KEY (parent_id)
    REFERENCES public.locations (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.points
    ADD CONSTRAINT points_report_id_foreign FOREIGN KEY (report_id)
    REFERENCES public.reports (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE SET NULL;


ALTER TABLE IF EXISTS public.points
    ADD CONSTRAINT points_user_id_foreign FOREIGN KEY (user_id)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.reports
    ADD CONSTRAINT reports_gedung_id_foreign FOREIGN KEY (gedung_id)
    REFERENCES public.gedungs (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE SET NULL;


ALTER TABLE IF EXISTS public.reports
    ADD CONSTRAINT reports_location_id_foreign FOREIGN KEY (location_id)
    REFERENCES public.locations (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE SET NULL;


ALTER TABLE IF EXISTS public.reports
    ADD CONSTRAINT reports_reporter_id_foreign FOREIGN KEY (reporter_id)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.reports
    ADD CONSTRAINT reports_reviewed_by_foreign FOREIGN KEY (reviewed_by)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE SET NULL;


ALTER TABLE IF EXISTS public.ruangans
    ADD CONSTRAINT program_studis_jurusan_id_foreign FOREIGN KEY (gedung_id)
    REFERENCES public.gedungs (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.users
    ADD CONSTRAINT users_gedung_id_foreign FOREIGN KEY (gedung_id)
    REFERENCES public.gedungs (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE SET NULL;


ALTER TABLE IF EXISTS public.warnings
    ADD CONSTRAINT warnings_created_by_foreign FOREIGN KEY (created_by)
    REFERENCES public.users (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;


ALTER TABLE IF EXISTS public.warnings
    ADD CONSTRAINT warnings_report_id_foreign FOREIGN KEY (report_id)
    REFERENCES public.reports (id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE CASCADE;

END;